plugins {
  id "org.sonarqube" version "2.8"
}

allprojects {
  apply plugin: 'java'
  apply plugin: 'java-library'
  apply plugin: 'maven-publish'
}

sonarqube {
  properties {
    property "sonar.projectKey", "tesseraktor_tesserakt"
  }
}
